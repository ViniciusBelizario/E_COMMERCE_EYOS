<% layout('layout') -%>
<section class="space-y-6">
  <h1 class="text-2xl font-semibold">Busca • Produto nome + marca</h1>

  <form method="GET" class="bg-white border rounded p-4 grid md:grid-cols-3 gap-4">
    <%- include('../components/formInput', { name:'q', label:'Nome', value:q || '', placeholder:'Ex.: camiseta' }) %>
    <%- include('../components/formInput', { name:'marca', label:'Marca', value:marca || '', placeholder:'Ex.: nike' }) %>
    <div class="md:col-span-3"><button class="btn">Buscar</button></div>
  </form>

  <%- include('../components/table', {
    cols: [
      { key:'id',   label:'ID' },
      { key:'nome', label:'Produto' }
    ],
    rows: items,
    basePath: '#'
  }) %>
</section>
