<% layout('layout') -%>
<section class="space-y-6">
  <h1 class="text-2xl font-semibold">Busca • Marca parcial</h1>

  <form method="GET" class="bg-white border rounded p-4 grid md:grid-cols-3 gap-4">
    <%- include('../components/formInput', { name:'nome', label:'Nome parcial', value:nome || '', placeholder:'Ex.: adi' }) %>
    <div class="md:col-span-3"><button class="btn">Buscar</button></div>
  </form>

  <%- include('../components/table', {
    cols: [
      { key:'id',   label:'ID' },
      { key:'nome', label:'Marca' }
    ],
    rows: items,
    basePath: '#'
  }) %>
</section>
